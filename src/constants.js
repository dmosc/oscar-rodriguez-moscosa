const maxDesktopWidth = 1000;
const dateFormat = 'DD/MM/YYYY';
const timeFormat = 'HH:mm';
const datetimeFormat = `${dateFormat} ${timeFormat}`;

export {maxDesktopWidth, dateFormat, timeFormat, datetimeFormat};
